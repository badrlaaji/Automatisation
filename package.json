{
  "name": "xstate-bpmn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal BPMN workflow engine built with XState v5",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "xstate": "^5.18.2"
  }
}
